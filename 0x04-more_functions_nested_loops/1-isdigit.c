#include "main.h"

/**
 * _isdigit - Prog that checks for a digit (0 through 9)
 * @c: Enter to be checked
 * Result: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
