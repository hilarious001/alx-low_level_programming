#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 (Success)
 */

int main(void)
{
	char z;

	char y;

	z = 'a';
	y = 'A';
	while
		(z <= 'z') {
			putchar(z);
			z++;
		}
	while
		(y <= 'Z') {
			putchar (y);
			y++;
		}
	putchar('\n');
	return (0);
}
