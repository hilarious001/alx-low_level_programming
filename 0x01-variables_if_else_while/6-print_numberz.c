#include <stdio.h>

/**
 * main - Program Entry point
 * Return: 0 (Success)
 */

int main(void)
{
	int h;

	h = 0;
	while
		(h < 10) {
			putchar(h + '0');
			h++;
		}
	putchar('\n');
	return (0);
}
