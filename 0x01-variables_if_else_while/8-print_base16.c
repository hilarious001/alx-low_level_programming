#include <stdio.h>

/**
 * main - Program Entry point
 * Return: 0 (Success)
 */

int main(void)
{
	char h;

	int g;

	h = 'a';
	g = 0;
	while
		(g < 10) {
			putchar(g + '0');
			g++;
		}
	while
		(h <= 'f') {
			putchar(h);
			h++;
		}
	putchar('\n');
	return (0);
}
